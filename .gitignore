# Browser research runtime data
research-runs/
browser-profiles/
browser-profile/
session-data/
cookies/
local-storage/
downloads/private/
exports/private/
*.private.csv
*.private.json
*.private.zip

# Bounded completion runtime state and evidence
.agent-loop/

# Common local artifacts
__pycache__/
*.pyc
.DS_Store
.env
.env.*
!.env.example

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Mobile release automation toolkit: local virtualenv and build artifacts.
# Credentials live in ~/.config/mobile-release-automation, never in the repo.
tools/mobile-release-automation/.venv/
tools/mobile-release-automation/*.egg-info/
